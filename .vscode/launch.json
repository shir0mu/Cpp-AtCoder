{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(LLDB) Debug C++",
            "type": "lldb",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}",
            "args": [],
            "cwd": "${fileDirname}",
            "preLaunchTask": "g++-16 build active file"
        }
    ]
}